# 🎨 Modular Architecture Visualization

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     FRONTEND                                │
│              (animal-sound-safari)                          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  src/config/gameConfig.ts                            │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │  mode: 'DATA_COLLECTION' | 'PREDICTION' | 'BOTH'  │  │
│  │  └────────────┬────────────────────────────────────┘  │  │
│  └───────────────┼───────────────────────────────────────┘  │
│                  │                                          │
│                  ▼                                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  src/services/gameService.ts                         │  │
│  │  • recordAttempt()                                   │  │
│  │  • getPrediction()                                   │  │
│  │  • Modular API calls based on mode                  │  │
│  └────────┬──────────────────────────┬──────────────────┘  │
│           │                          │                     │
└───────────┼──────────────────────────┼─────────────────────┘
            │                          │
            │                          │
    ┌───────▼──────────┐      ┌────────▼────────┐
    │                  │      │                 │
    │   app.py         │      │ prediction_     │
    │   Port 5000      │      │ api.py          │
    │                  │      │ Port 5001       │
    │ DATA_COLLECTION  │      │ PREDICTION      │
    │      BOTH        │      │     BOTH        │
    │                  │      │                 │
    └────────┬─────────┘      └────────┬────────┘
             │                         │
             ▼                         ▼
    ┌─────────────────┐      ┌──────────────────┐
    │ game_logs.json  │      │ session_storage  │
    │ game_analytics  │      │ {} (RAM)         │
    │ .csv            │      │                  │
    │ (Permanent)     │      │ (Temporary)      │
    └─────────────────┘      └──────────────────┘
```

---

## 🔀 Mode-Based Flow Diagrams

### Mode 1: DATA_COLLECTION

```
┌──────────┐
│ Frontend │
│  (React) │
└────┬─────┘
     │
     │ Child clicks animal
     │
     ▼
┌─────────────────────┐
│ gameService.ts      │
│                     │
│ shouldCallAppAPI()  │
│ → true              │
│                     │
│ shouldCall          │
│ PredictionAPI()     │
│ → false             │
└────┬────────────────┘
     │
     │ POST request
     │
     ▼
┌─────────────────────┐
│   app.py            │
│   Port 5000         │
│                     │
│ /api/log-interaction│
└────┬────────────────┘
     │
     │ Save data
     │
     ▼
┌─────────────────────┐
│ game_analytics.csv  │
│ game_logs.json      │
│                     │
│ PERMANENT STORAGE   │
└─────────────────────┘

Features:
✅ Data collection only
❌ No predictions
❌ No prediction button in UI
```

---

### Mode 2: PREDICTION

```
┌──────────┐
│ Frontend │
│  (React) │
└────┬─────┘
     │
     │ Child clicks animal
     │
     ▼
┌─────────────────────┐
│ gameService.ts      │
│                     │
│ shouldCallAppAPI()  │
│ → false             │
│                     │
│ shouldCall          │
│ PredictionAPI()     │
│ → true              │
└────┬────────────────┘
     │
     │ POST request
     │
     ▼
┌─────────────────────┐
│ prediction_api.py   │
│ Port 5001           │
│                     │
│ /api/predict/record │
└────┬────────────────┘
     │
     │ Store in RAM
     │
     ▼
┌─────────────────────┐
│ session_storage{}   │
│                     │
│ TEMPORARY (RAM)     │
└─────────────────────┘
     │
     │ After 5+ attempts
     │
     ▼
┌─────────────────────┐
│ User clicks         │
│ "Get Assessment"    │
└────┬────────────────┘
     │
     │ POST /analyze
     │
     ▼
┌─────────────────────┐
│ prediction_api.py   │
│                     │
│ • Calculate features│
│ • Load ML model     │
│ • Make prediction   │
└────┬────────────────┘
     │
     │ Return result
     │
     ▼
┌─────────────────────┐
│ Frontend displays   │
│ prediction modal    │
│                     │
│ • Risk level        │
│ • Probability       │
│ • Recommendation    │
└─────────────────────┘

Features:
❌ No permanent storage
✅ Real-time predictions
✅ Prediction button visible
```

---

### Mode 3: BOTH

```
┌──────────┐
│ Frontend │
│  (React) │
└────┬─────┘
     │
     │ Child clicks animal
     │
     ▼
┌─────────────────────────────┐
│ gameService.ts              │
│                             │
│ shouldCallAppAPI() → true   │
│ shouldCallPredictionAPI()   │
│ → true                      │
│                             │
│ Promise.all([...])          │
└────┬────────────────────────┘
     │
     ├──────────────┬──────────────┐
     │              │              │
     ▼              ▼              │
┌─────────┐  ┌─────────────┐      │
│ app.py  │  │ prediction_ │      │
│ :5000   │  │ api.py      │      │
│         │  │ :5001       │      │
└────┬────┘  └──────┬──────┘      │
     │              │              │
     ▼              ▼              │
┌─────────┐  ┌─────────────┐      │
│CSV/JSON │  │session_     │      │
│files    │  │storage{}    │      │
│         │  │             │      │
│PERMANENT│  │TEMPORARY    │      │
└─────────┘  └──────┬──────┘      │
                    │              │
                    │ After 5+     │
                    ▼              │
              ┌─────────────┐      │
              │Get          │      │
              │Prediction   │      │
              └─────────────┘      │
                                  │
                ┌─────────────────┘
                │
                ▼
       ┌────────────────────┐
       │ Frontend shows:    │
       │ • Score            │
       │ • Attempts         │
       │ • Prediction btn   │
       │ • Results modal    │
       └────────────────────┘

Features:
✅ Permanent storage (app.py)
✅ Real-time predictions (prediction_api.py)
✅ All features enabled
```

---

## 🎛️ Configuration State Machine

```
┌─────────────────────────────────────────────────────┐
│         src/config/gameConfig.ts                    │
└──────────────────┬──────────────────────────────────┘
                   │
                   │ mode: ?
                   │
        ┌──────────┼──────────┐
        │          │          │
        ▼          ▼          ▼
   ┌────────┐ ┌────────┐ ┌────────┐
   │  DATA_ │ │PREDICT-│ │  BOTH  │
   │COLLECT-│ │  ION   │ │        │
   │  ION   │ │        │ │        │
   └───┬────┘ └───┬────┘ └───┬────┘
       │          │          │
       ▼          ▼          ▼
   ┌────────┐ ┌────────┐ ┌────────┐
   │app.py  │ │predict-│ │app.py  │
   │only    │ │ion_api │ │   +    │
   │        │ │only    │ │predict-│
   │        │ │        │ │ion_api │
   └────────┘ └────────┘ └────────┘
```

---

## 🔍 Feature Matrix

```
┌──────────────────────────────────────────────────────────────┐
│                    FEATURE MATRIX                            │
├──────────────────┬──────────┬────────────┬──────────────────┤
│ Feature          │   DATA_  │ PREDICTION │      BOTH        │
│                  │COLLECTION│            │                  │
├──────────────────┼──────────┼────────────┼──────────────────┤
│ Calls app.py     │    ✅    │     ❌     │       ✅         │
│ Calls            │          │            │                  │
│ prediction_api   │    ❌    │     ✅     │       ✅         │
│ Saves CSV/JSON   │    ✅    │     ❌     │       ✅         │
│ Shows prediction │    ❌    │     ✅     │       ✅         │
│ Prediction btn   │  Hidden  │  Visible   │    Visible       │
│ Data lifetime    │Permanent │ Temporary  │Both (Perm+Temp)  │
│ Backends needed  │    1     │     1      │       2          │
│ Best for         │ Training │ Production │   Development    │
└──────────────────┴──────────┴────────────┴──────────────────┘
```

---

## 📊 Data Storage Comparison

```
DATA_COLLECTION Mode:
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   app.py    │
└──────┬──────┘
       │
       ├─────────────────┐
       │                 │
       ▼                 ▼
┌─────────────┐   ┌─────────────┐
│game_logs    │   │game_        │
│.json        │   │analytics.csv│
│             │   │             │
│• Complete   │   │• ML training│
│  audit log  │   │  format     │
│• All fields │   │• Session    │
│• JSON array │   │  based      │
└─────────────┘   └─────────────┘
   PERMANENT         PERMANENT


PREDICTION Mode:
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│prediction_  │
│api.py       │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│session_     │
│storage = {} │
│             │
│• Python dict│
│• In RAM     │
│• Lost on    │
│  restart    │
└─────────────┘
   TEMPORARY


BOTH Mode:
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       ├─────────────────┐
       │                 │
       ▼                 ▼
┌─────────────┐   ┌─────────────┐
│   app.py    │   │prediction_  │
│             │   │api.py       │
└──────┬──────┘   └──────┬──────┘
       │                 │
       ▼                 ▼
┌─────────────┐   ┌─────────────┐
│CSV/JSON     │   │session_     │
│files        │   │storage{}    │
│             │   │             │
│PERMANENT    │   │TEMPORARY    │
└─────────────┘   └─────────────┘
```

---

## 🚦 Decision Tree: Which Mode to Use?

```
                  START
                    │
                    ▼
         ┌──────────────────────┐
         │ Do you have a        │
         │ trained model?       │
         └──┬────────────────┬──┘
            │                │
         NO │                │ YES
            │                │
            ▼                ▼
    ┌──────────────┐  ┌──────────────────┐
    │ Need to      │  │ Want to save     │
    │ collect data │  │ new data too?    │
    └──────┬───────┘  └──┬───────────┬───┘
           │             │           │
           ▼          YES│           │NO
    ┌──────────────┐     │           │
    │ DATA_        │     ▼           ▼
    │ COLLECTION   │  ┌─────┐    ┌─────────┐
    │              │  │BOTH │    │PREDICTION│
    │ • Run app.py │  │     │    │         │
    │ • Collect    │  │• Run│    │• Run    │
    │   300+ tries │  │  all│    │  predict│
    │ • Train      │  │• Get│    │• Fast   │
    │   later      │  │ both│    │• Light  │
    └──────────────┘  └─────┘    └─────────┘
```

---

This visualization should help you understand how the modular system works! 🎯
